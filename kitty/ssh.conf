# apply to all hosts; tighten with hostname later if needed
hostname *
# macOS sometimes needs python bootstrap
interpreter python3
# force shell integration on
shell_integration enabled
# optional: if login shell detection is weird
login_shell /bin/zsh
