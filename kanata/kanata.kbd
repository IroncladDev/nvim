(defcfg 
  process-unmapped-keys yes
  danger-enable-cmd yes
  concurrent-tap-hold yes
  linux-dev-names-include ( "beekeeb Piantor Pro" )
)

(defvar
  tap-time 175
  hold-time 125
  combo-time 25
)

(include combos.kbd)
(include tap-hold.kbd)
(include sequences.kbd)
(include overrides.kbd)

(defsrc
  grv  1    2    3    4    5    6    7    8    9    0    -    =    bspc
  tab  q    w    e    r    t    y    u    i    o    p    [    ]    \
  esc  a    s    d    f    g    h    j    k    l    ;    '    ent
  lsft z    x    c    v    b    n    m    ,    .    /    rsft
  lctl lmet lalt           spc            ralt rmet rctl
)

(deflayer colmak
  _    _    _    _    _    _    _    _    _    _    _    _    _    @bsc
  _    q    w    f    p    b    j    l    u    y    ;    _    _    _
  _    a    r    s    t    g    m    n    e    i    o    _    @rly
  @cdr z    x    c    d    v    k    h    ,    .    /    _
  _    _    _              @spl           _    _    _
)
